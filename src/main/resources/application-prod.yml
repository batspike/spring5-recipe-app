spring:
    datasource:
        url: jdbc:mysql://localhost:3306/sfg_prod?useSSL=false&serverTimezone=UTC
        username: sfg_prod_user
        password: guru

    jpa:
        hibernate:
            ddl-auto:  validate
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        database: MYSQL
        show-sql: true
